import { AdminPlaceholder } from "../_placeholder";

export default function KnownIssuesPage() {
  return <AdminPlaceholder name="Known issues" blurb="Incident banners shown at the top of the employee dashboard." />;
}
