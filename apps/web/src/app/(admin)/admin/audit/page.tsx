import { AdminPlaceholder } from "../_placeholder";

export default function AuditPage() {
  return <AdminPlaceholder name="Audit log" blurb="Actor, action, entity, before → after, with filters and JSON diffs." />;
}
