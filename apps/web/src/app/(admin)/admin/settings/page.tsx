import { AdminPlaceholder } from "../_placeholder";

export default function SettingsPage() {
  return <AdminPlaceholder name="Settings" blurb="Auto-release threshold, business hours, holidays, and canned responses." />;
}
